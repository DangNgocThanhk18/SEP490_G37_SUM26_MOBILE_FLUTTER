# Firebase client API keys are public project identifiers, not authorization
# credentials. These two exact native config files are intentionally committed
# so Android/iOS clones build against the correct Firebase project. Never add a
# Firebase Admin SDK service-account JSON or private key to either path.
paths-ignore:
  - "android/app/google-services.json"
  - "ios/Runner/GoogleService-Info.plist"
